# Lesson 22: PCAP Packet Capture System

## 概述

本示例实现了一个高性能的数据包捕获系统,支持 PCAPNG 格式录制,与 Wireshark 完全兼容。

## 核心功能

### 1. 多种捕获模式
- **全量捕获**: 录制所有数据包
- **采样捕获**: 按比例采样 (如 1%)
- **条件捕获**: 只录制特定类型的包 (TCP SYN, ICMP 等)

### 2. 高性能设计
- **异步写入**: 独立线程写入,不阻塞收包
- **Ring 队列缓冲**: 避免写入延迟导致丢包
- **批量写入**: 减少系统调用开销
- **多队列并行**: 支持多核收包

### 3. 文件管理
- **自动轮转**: 按大小 (1GB) 或时间 (1小时)
- **时间戳命名**: capture_YYYYmmdd_HHMMSS_NNN.pcapng
- **统计信息**: 每个文件包含完整的统计数据

### 4. 实时监控
- 捕获统计 (总包数、已捕获、丢弃)
- 文件信息 (大小、已创建文件数)
- 写入队列状态
- 捕获率和丢包率

## 编译

```bash
cd build
cmake ..
make pcap_capture
```

## 运行示例

### 基本用法

```bash
# 全量捕获模式 (默认)
sudo ./bin/pcap_capture -l 0-2

# 采样捕获模式 (1% 采样率)
sudo ./bin/pcap_capture -l 0-2 -- -m 1 -s 100

# 条件捕获模式 (只录 TCP SYN + ICMP)
sudo ./bin/pcap_capture -l 0-2 -- -m 2
```

### 命令行参数

```
选项:
  -m MODE    捕获模式:
               0 = 全量捕获 (默认)
               1 = 采样捕获
               2 = 条件捕获 (TCP SYN + ICMP)

  -s RATE    采样率 (默认: 100, 即 1/100)
               100 = 1% 采样
               10 = 10% 采样
               1 = 100% (全量)

  -h         显示帮助
```

## 输出示例

```
╔════════════════════════════════════════════════════════╗
║   DPDK PCAP Capture Monitoring                         ║
╚════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════╗
║         PCAP Capture Statistics                        ║
╚════════════════════════════════════════════════════════╝

Packet Counts:
  Total Received:         1000000
  Captured:               1000000 (100.0%)
  Dropped:                     50 (0.0%)

File Information:
  Current File:     capture_20251218_072015_001.pcapng
  Current Size:     456.78 MB
  Total Written:    0.45 GB
  Files Created:    1

Capture Mode:
  Mode: Full Capture

Write Queue:
  Pending:          12
  Free Space:       4084
```

## 捕获文件分析

### 使用 Wireshark

```bash
# 图形界面打开
wireshark capture_20251218_072015_001.pcapng

# 过滤 HTTP 流量
wireshark -Y "tcp.port==80" capture_*.pcapng
```

### 使用 tshark (命令行)

```bash
# 查看文件信息
capinfos capture_*.pcapng

# 统计协议分布
tshark -r capture_*.pcapng -q -z io,phs

# 提取 TCP 流
tshark -r capture_*.pcapng -Y "tcp.stream eq 0" -w stream0.pcapng

# 统计 Top 10 IP
tshark -r capture_*.pcapng -q -z ip_hosts,tree
```

### 使用 tcpdump

```bash
# 基本查看
tcpdump -r capture_*.pcapng

# 过滤特定流量
tcpdump -r capture_*.pcapng 'tcp port 443'

# 详细输出
tcpdump -vvv -r capture_*.pcapng
```

### 使用 Python (Scapy)

```python
from scapy.all import *

# 读取 PCAP 文件
pkts = rdpcap("capture_20251218_072015_001.pcapng")

# 统计
print(f"Total packets: {len(pkts)}")

# 协议分布
tcp = len([p for p in pkts if TCP in p])
udp = len([p for p in pkts if UDP in p])
print(f"TCP: {tcp}, UDP: {udp}")

# 提取 HTTP 流量
http_pkts = [p for p in pkts if TCP in p and (p[TCP].dport == 80 or p[TCP].sport == 80)]
wrpcap("http_only.pcapng", http_pkts)

# Top 10 源 IP
from collections import Counter
src_ips = [p[IP].src for p in pkts if IP in p]
top_ips = Counter(src_ips).most_common(10)
for ip, count in top_ips:
    print(f"{ip}: {count} packets")
```

## 捕获策略选择

### 全量捕获 (Mode 0)

```
适用场景:
  ✓ 短期调试和问题诊断
  ✓ 完整的取证分析
  ✓ 小流量环境 (< 100 Mbps)

优点: 完整记录,不遗漏任何包
缺点: 存储需求大,高速流量下可能写不过来

存储需求估算:
  1 Gbps = 125 MB/s = 450 GB/hour
  10 Gbps = 1.25 GB/s = 4.5 TB/hour
```

### 采样捕获 (Mode 1)

```
适用场景:
  ✓ 长期监控
  ✓ 高速流量环境 (1-10 Gbps)
  ✓ 存储空间有限

优点: 大幅减少存储,可长期运行
缺点: 可能错过特定事件

推荐采样率:
  1% (1/100):  适合 10 Gbps 流量
  5% (1/20):   适合 5 Gbps 流量
  10% (1/10):  适合 1 Gbps 流量
```

### 条件捕获 (Mode 2)

```
适用场景:
  ✓ 安全监控 (只录可疑流量)
  ✓ 特定协议分析
  ✓ 事件触发录制

当前过滤条件:
  • TCP SYN 包 (新连接建立)
  • ICMP 包 (网络诊断)

优点: 精准捕获关键流量
缺点: 需要预先定义过滤条件
```

## 性能数据

### 写入性能

```
硬件配置对性能的影响:
┌────────────────┬──────────────┬─────────────┐
│ 存储设备       │ 最大写入速率 │ IOPS        │
├────────────────┼──────────────┼─────────────┤
│ HDD (7200rpm)  │ ~100 Mbps    │ ~100        │
│ SATA SSD       │ ~1 Gbps      │ ~10K        │
│ NVMe SSD       │ ~5-10 Gbps   │ ~100K+      │
│ RAM Disk       │ ~40 Gbps     │ 无限制      │
└────────────────┴──────────────┴─────────────┘

优化建议:
  1. 使用 NVMe SSD 存储
  2. 采样录制 (1-10%)
  3. 异步写入 (已实现)
  4. 增大写入队列
```

### CPU 开销

```
捕获模式的 CPU 开销:
┌─────────────────┬──────────────┐
│ 模式            │ CPU 开销     │
├─────────────────┼──────────────┤
│ 全量捕获        │ +20-30%      │
│ 采样捕获 (1%)   │ +2-5%        │
│ 条件捕获        │ +5-10%       │
└─────────────────┴──────────────┘
```

## 文件轮转

### 按大小轮转

```c
#define MAX_CAPTURE_SIZE (1024 * 1024 * 1024)  // 1GB

当前文件达到 1GB 时自动创建新文件
```

### 按时间轮转

```c
#define ROTATE_INTERVAL_SEC 3600  // 1 小时

每小时创建一个新文件
```

### 文件命名规则

```
格式: capture_YYYYmmdd_HHMMSS_NNN.pcapng

示例:
  capture_20251218_072015_001.pcapng
  capture_20251218_072015_002.pcapng
  capture_20251218_082015_003.pcapng

好处:
  • 按时间排序
  • 易于定位特定时段的流量
  • 支持批量处理
```

## 自定义过滤条件

修改 `should_capture()` 函数即可实现自定义过滤:

```c
static int should_capture(struct rte_mbuf *m)
{
    /* 示例1: 只捕获来自特定 IP 的流量 */
    if (src_ip == 0xC0A80164) {  // 192.168.1.100
        return 1;
    }

    /* 示例2: 只捕获 DNS 流量 */
    if (protocol == IPPROTO_UDP && (src_port == 53 || dst_port == 53)) {
        return 1;
    }

    /* 示例3: 只捕获异常大小的包 */
    if (pkt_len > 9000) {  // Jumbo frame
        return 1;
    }

    /* 示例4: 只捕获特定 MAC 地址 */
    if (memcmp(src_mac, vip_mac, 6) == 0) {
        return 1;
    }

    return 0;
}
```

## 实用脚本

### 自动分析脚本

```bash
#!/bin/bash
# analyze_capture.sh

PCAP_FILE=$1

echo "=== PCAP File Analysis ==="
echo

echo "File Information:"
capinfos $PCAP_FILE

echo
echo "Protocol Hierarchy:"
tshark -r $PCAP_FILE -q -z io,phs

echo
echo "Top 10 Conversations:"
tshark -r $PCAP_FILE -q -z conv,ip

echo
echo "Top 10 Talkers:"
tshark -r $PCAP_FILE -q -z ip_hosts,tree | head -15
```

### 自动清理脚本

```bash
#!/bin/bash
# cleanup_old_captures.sh

RETENTION_DAYS=7
CAPTURE_DIR="./captures"

find $CAPTURE_DIR -name "*.pcapng" -mtime +$RETENTION_DAYS -delete
echo "Cleaned up captures older than $RETENTION_DAYS days"
```

## 故障排查

### 问题1: 写入队列满,丢包

```
现象: cap_stats.dropped_packets 持续增长

原因: 写盘速度跟不上收包速度

解决方案:
  1. 增大 WRITE_RING_SIZE (默认 4096)
  2. 使用更快的存储设备 (NVMe)
  3. 降低采样率
  4. 使用条件捕获
```

### 问题2: 文件过大

```
现象: 单个文件数 GB

原因: 流量大且未轮转

解决方案:
  1. 减小 MAX_CAPTURE_SIZE
  2. 减小 ROTATE_INTERVAL_SEC
  3. 使用采样捕获
```

### 问题3: 磁盘空间不足

```
解决方案:
  1. 实施自动清理策略
  2. 使用压缩存储
  3. 增加存储容量
  4. 降低采样率
```

## 与其他工具集成

### 与 Suricata IDS 集成

```bash
# DPDK 捕获流量
sudo ./pcap_capture -l 0-2 -- -m 0

# Suricata 读取 PCAP 离线分析
suricata -r capture_*.pcapng -l ./logs/
```

### 与 Zeek (Bro) 集成

```bash
# 使用 Zeek 分析捕获文件
zeek -r capture_*.pcapng local.zeek

# 查看生成的日志
cat conn.log http.log dns.log
```

### 与 Moloch 集成

```bash
# Moloch 导入 PCAP
moloch-capture -r capture_*.pcapng
```

## 性能优化建议

### 1. 存储优化

```bash
# 使用 RAM Disk (临时存储)
sudo mkdir -p /mnt/ramdisk
sudo mount -t tmpfs -o size=4G tmpfs /mnt/ramdisk
cd /mnt/ramdisk && sudo /path/to/pcap_capture -l 0-2

# 定期移动到持久存储
rsync -av /mnt/ramdisk/*.pcapng /data/captures/
```

### 2. CPU 优化

```bash
# 绑定核心,避免进程迁移
sudo taskset -c 0-3 ./pcap_capture -l 0-3

# 设置高优先级
sudo nice -n -20 ./pcap_capture -l 0-3
```

### 3. 网卡优化

```bash
# 增大网卡 Ring Buffer
sudo ethtool -G eth0 rx 4096 tx 4096

# 关闭不必要的 offload
sudo ethtool -K eth0 gro off lro off
```

## 应用场景

### 场景1: 安全取证

```
捕获配置:
  - 模式: 条件捕获
  - 过滤: TCP SYN + ICMP + DNS
  - 轮转: 每小时
  - 保留: 7 天

用途:
  • 攻击流量溯源
  • 异常行为分析
  • 合规性审计
```

### 场景2: 性能诊断

```
捕获配置:
  - 模式: 采样捕获 (1%)
  - 轮转: 每 1GB
  - 保留: 24 小时

用途:
  • 应用性能分析
  • 网络延迟测量
  • 丢包原因诊断
```

### 场景3: 流量回放测试

```
捕获配置:
  - 模式: 全量捕获
  - 时长: 5-10 分钟
  - 无轮转

用途:
  • 重现生产环境问题
  • 压力测试
  • 回归测试
```

## 相关文档

- [docs/lessons/lesson22-pcap-capture.md](../docs/lessons/lesson22-pcap-capture.md) - PCAP 录制详细教程
- DPDK Programmer's Guide - Packet Capture Library
- PCAPNG Specification
- Wireshark User Guide

## 总结

### 核心特性

- ✅ PCAPNG 格式 (Wireshark 兼容)
- ✅ 3 种捕获模式 (全量/采样/条件)
- ✅ 异步写入 (高性能)
- ✅ 自动文件轮转
- ✅ 实时统计监控
- ✅ 可自定义过滤条件

### 典型性能

- **全量捕获**: 1-5 Gbps (取决于存储)
- **采样捕获**: 10+ Gbps (1% 采样)
- **CPU 开销**: 5-30% (取决于模式)
- **丢包率**: < 0.1% (NVMe 存储)

### 实际应用

现在你可以:
1. 🎯 捕获 10Gbps 高速流量
2. 🔍 Wireshark 离线分析
3. 🛡️ 安全事件取证
4. 📊 性能问题诊断
5. 🔄 流量回放测试

你已经掌握了企业级网络流量捕获系统的完整技能!
